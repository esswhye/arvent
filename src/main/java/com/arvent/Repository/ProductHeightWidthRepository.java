package com.arvent.Repository;

import com.arvent.Entity.ProductHeightWidth;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductHeightWidthRepository extends JpaRepository<ProductHeightWidth, Long> {
}
